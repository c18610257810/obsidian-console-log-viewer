/**
 * Core module exports
 */

export { ConsoleHook, hookConsole, restoreConsole, getConsoleHook } from './ConsoleHook';
export { LogEntry, LogLevel, ConsoleHookConfig, ConsoleMethodName, OriginalConsole } from './types';